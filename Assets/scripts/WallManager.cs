using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallManager : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //todo -> toda parede teria um script com um id, identificando se é uma parede do player 1 ou 2 (?)
    public void toggleWallVisibility(int playerId)
    {

    }
}
